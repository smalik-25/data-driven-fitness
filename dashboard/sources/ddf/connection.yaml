name: ddf
type: duckdb
options:
  # The committed, de-identified public DB (built by `make export-public`).
  # Resolved relative to this source folder (sources/ddf/). Local dev and Netlify
  # CI both read this — the full warehouse stays gitignored and local.
  filename: public.duckdb
