# Entity-Relationship / Schema

> Filled in during Phase 2-3. Star schema: `fct_daily` (date grain) +
> `dim_date`, `dim_exercise` -> muscle_group, `dim_region`, and benchmark
> dims `dim_nhanes_bodycomp`, `dim_acsm_targets`. Mermaid diagram to follow.
